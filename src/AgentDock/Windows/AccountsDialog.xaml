<Window x:Class="AgentDock.Windows.AccountsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Claude Accounts"
        Width="640" Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Icon="/Assets/agentdock.png"
        Background="Transparent">

    <Window.Resources>
        <Style x:Key="DialogButton" TargetType="{x:Type Button}">
            <Setter Property="MinWidth" Value="76" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Foreground" Value="{DynamicResource HelpForeground}" />
            <Setter Property="Background" Value="{DynamicResource HelpCardBackground}" />
            <Setter Property="BorderBrush" Value="{DynamicResource HelpCardBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource MenuHighlightBackground}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                            <Trigger Property="IsDefault" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush"
                                        Value="{DynamicResource TabButtonActiveBorderBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FieldHint" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Foreground" Value="{DynamicResource HelpForeground}" />
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource HelpBackground}"
            BorderBrush="{DynamicResource ToolbarBorderBrush}"
            BorderThickness="1"
            CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0"
                    Background="{DynamicResource TitleBarBackground}"
                    CornerRadius="4,4,0,0"
                    Padding="12,8"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock Text="Claude Accounts"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TitleBarForeground}" />
            </Border>

            <!-- Intro -->
            <TextBlock Grid.Row="1"
                       Style="{StaticResource FieldHint}"
                       Margin="20,14,20,6"
                       Text="Each account is a separate Claude login. When you start a session you can pick which account it uses, letting you run more than one Claude plan at once across projects. Add an account, sign in once in the terminal that opens, then click Refresh." />

            <!-- Account list -->
            <Border Grid.Row="2"
                    Margin="20,6,20,6"
                    BorderBrush="{DynamicResource HelpCardBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="3">
                <Grid>
                    <ListBox x:Name="AccountsList"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="{DynamicResource HelpForeground}"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="6,6">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource HelpForeground}" />
                                        <TextBlock Text="{Binding StatusText}"
                                                   FontSize="12"
                                                   Margin="10,1,0,0"
                                                   Foreground="{DynamicResource HelpForeground}" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding ConfigDir}"
                                               FontSize="10"
                                               Opacity="0.7"
                                               Foreground="{DynamicResource HelpForeground}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <TextBlock x:Name="EmptyHint"
                               Text="No accounts yet. Add one below to get started."
                               Style="{StaticResource FieldHint}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="Collapsed" />
                </Grid>
            </Border>

            <!-- Per-account actions + add row -->
            <StackPanel Grid.Row="3" Margin="20,0,20,12">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="Log In / Re-authenticate"
                            Style="{StaticResource DialogButton}"
                            Click="LogInButton_Click"
                            Margin="0,0,8,0" />
                    <Button Content="Refresh"
                            Style="{StaticResource DialogButton}"
                            Click="RefreshButton_Click"
                            Margin="0,0,8,0" />
                    <Button Content="Remove"
                            Style="{StaticResource DialogButton}"
                            Click="RemoveButton_Click" />
                </StackPanel>

                <DockPanel>
                    <Button DockPanel.Dock="Right"
                            Content="Add Account"
                            Style="{StaticResource DialogButton}"
                            Click="AddButton_Click"
                            Margin="8,0,0,0" />
                    <TextBox x:Name="NewAccountName"
                             Height="28"
                             FontSize="13"
                             VerticalContentAlignment="Center"
                             Foreground="{DynamicResource HelpForeground}"
                             Background="{DynamicResource HelpCardBackground}"
                             BorderBrush="{DynamicResource HelpCardBorderBrush}"
                             BorderThickness="1"
                             Padding="6,0"
                             KeyDown="NewAccountName_KeyDown" />
                </DockPanel>
                <TextBlock Style="{StaticResource FieldHint}"
                           Margin="0,4,0,0"
                           Text="Give the new account a name (e.g. Personal or Work), then sign in when the terminal opens." />
            </StackPanel>

            <!-- Footer -->
            <Border Grid.Row="4"
                    BorderBrush="{DynamicResource ToolbarBorderBrush}"
                    BorderThickness="0,1,0,0"
                    Padding="16,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Close"
                            Style="{StaticResource DialogButton}"
                            IsCancel="True"
                            Click="CloseButton_Click" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
